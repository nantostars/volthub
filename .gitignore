# PlatformIO
.pio/
.pioenvs/
.piolibdeps/

# macOS
.DS_Store

# Claude local settings (per-machine)
.claude/settings.local.json

# Build / temp
*.bin
*.elf
*.map

# Demo/reference del vendor (materiale di riferimento, non serve al build)
JC3248W535EN/

# ESP-IDF component manager auto-generated manifest
src/idf_component.yml
*.orig

# Original device photos (multi-MB, with camera EXIF) — only the processed
# versions in docs/screenshots/ are tracked
device-pics/
