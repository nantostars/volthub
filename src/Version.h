#pragma once
// ─── Firmware version ─────────────────────────────────────────────────────────
// Change-based versioning: bump on EVERY commit and add a matching entry to
// CHANGELOG.md (repo root). Scheme "0.N" (N = change number) until the 1.0 release.
// Shown in the System screen (device) and System tab (web, via /api/data "sys.fw").
#define FW_VERSION "0.55"
